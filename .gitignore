# Ignore temporary flycheck outputs generated by editors (rust-analyzer / flycheck)
**/flycheck0/**

# Typical target folders (optional - already often ignored by global gitignore)
/target/

# Node modules (if any JS/TS code)
/node_modules/
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
# ignore all env variants by default to avoid committing secrets
.env*
# allow example env files to be tracked in the repo (useful as templates)
!.env.example

# anchor-specific envs
anchor/.env*
!anchor/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Anchor
/anchor/.anchor
/anchor/target/debug
/anchor/target/deploy
/anchor/target/release
/anchor/target/sbf-solana-solana
/anchor/target/test-ledger
/anchor/target/.rustc_info.json
test-ledger
**/*.rs.bk

# ignore keypair files (common for test ledgers and local keys)
*-keypair.json
*.keypair.json

# editor/project files
.vscode/
.idea/
